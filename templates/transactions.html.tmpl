<h3 class="text-2xl font-bold mb-4">Sum: {{ .Sum }}</h3>
{{ range .Transactions }}
<div class="my-4 p-4 border rounded shadow">
    <p class="font-bold">{{ .Amount }}</p>
    <p class="text-sm text-gray-500">{{ .Description }}</p>
    <p class="text-xs text-gray-400">{{ .UserEmail }}</p>
</div>
{{ end }}
