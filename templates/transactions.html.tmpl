<h3 class="text-2xl font-bold mb-4">Sum: {{ .Sum }}</h3>
{{ range .Transactions }}
<div class="my-4 p-4 border rounded shadow{{  if lt .Amount 0 }} bg-red-100{{ else }} bg-green-100{{ end }}">
    <p class="font-bold">{{ .Amount }} NOK</p>
    <p class="text-sm text-gray-500">{{ .Description }}</p>
</div>
{{ end }}
